[project]
name = "uwu-tester"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "browser-use>=0.1.0",
    "langchain-anthropic>=0.3.0",
    "playwright>=1.40.0",
]

[tool.uv]
dev-dependencies = []
